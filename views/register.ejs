<!DOCTYPE html>
<html lang="en">
<head>

  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Souflydev | Account Registration</title>
  <link rel="stylesheet" href="/css/_login.css">
  <!-- seo optimization  -->
    <meta name="description" content="Create your Souflydev account to access your web and mobile project dashboard.">
    <link rel="canonical" href="https://soulfyhub.fr/register">
  <meta name="keywords" content="Portfolio, Web and mobile application developer">
  <meta property="og:title" content="Web and mobile application developer">
  <meta property="og:description" content="Web and mobile application developer">
    <meta property="og:image" content="./assets/images/upload/soulfydev.webp">
  <meta property="og:url" content="https://soulfyhub.fr">
  <meta property="og:type" content="website">
  <meta property="og:locale" content="en_US">
  <meta property="og:site_name" content="Web and mobile application developer">
  <meta property="og:image:width" content="1920">
  <meta property="og:image:height" content="1080">
  <meta property="og:image:alt" content="Web and mobile application developer">
  <meta property="og:image:type" content="image/jpeg">
    <meta property="og:image:secure_url" content="/images/upload/soulfydev.webp">
  <meta property="og:image:secure_url:width" content="1920">
  <meta property="og:image:secure_url:height" content="1080">
  <meta property="og:image:secure_url:alt" content="Web and mobile application developer">
  <meta property="og:image:secure_url:type" content="image/jpeg">
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="Web and mobile application developer">
  <meta name="twitter:description" content="Web and mobile application developer">
    <meta name="twitter:image" content="/images/upload/soulfydev.webp">
  <meta name="twitter:image:width" content="1920">
  <meta name="twitter:image:height" content="1080">
  <meta name="twitter:image:alt" content="Web and mobile application developer">
  <meta name="twitter:image:type" content="image/jpeg">
  <meta name="twitter:site" content="@soulfydev">
  <meta name="twitter:creator" content="@soulfydev">
  <meta name="author" content="Maxime Jumel">
  <meta name="robots" content="index, follow">
  <meta name="language" content="English">
  <meta name="rating" content="general">
  <meta name="distribution" content="global">
  <meta name="category" content="Portfolio">
    <meta name="geo.region" content="FR-IDF">
    <meta name="geo.placename" content="Coulommiers">
    <meta name="geo.position" content="48.8096;3.0822">
    <meta name="ICBM" content="48.8096, 3.0822">
  <!-- finish seo optimization  -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css">
</head>

<body class=" bg-black d-flex align-items-center justify-content-center">
<main class="login-container">
        <!-- Back Link -->
        <div class="mb-4">
            <a href="/" class="back-link">
                <i class="bi bi-arrow-left"></i> Back to home
            </a>
        </div>

        <!-- Card wrapper -->
        <section class="login-card text-center">
            <div class="mb-4">
                <img src="/images/upload/soulfydev.webp" class="brand-logo mb-3" alt="Soulfydev Logo">
                <h1 class="h3 font-weight-bold text-light mb-1">Create Your Souflydev Account</h1>
                <p class="text-light small pt-4">Access your Soulfydev dashboard</p>
                <div><a href="/login" class="back-link">
                    <i class="bi bi-arrow-right"></i> Login
                </a></div>
            </div>
            
            <!-- Message d'erreur / succès -->
            <div id="message-container" class="mb-3" style="min-height: 24px;">
                <% if (typeof message !== 'undefined' && message) { %>
                    <div class="alert alert-<%= message.type %> py-2 px-3 small mb-0" role="alert">
                        <i class="bi <%= message.icon || '' %> me-1">
                            <% if(message.type === "success") { %>
                                <i class="bi bi-check-circle-fill"></i>
                            <% } else { %>
                                <i class="bi bi-exclamation-triangle-fill"></i>
                            <% } %>
                        </i>
                        <%= message.text %>
                    </div>
                <% } %>
            </div>

            <!-- Register Form -->
            <form id="registerForm">
                <div class="mb-3 text-start">
                    <label for="inputPseudo" class="form-label text-light small font-weight-bold">Pseudo</label>
                    <input type="text" class="form-control form-control-custom" id="inputPseudo" placeholder="Votre pseudo" autocomplete="username" required>
                </div>
                
                <div class="mb-3 text-start">
                    <label for="inputEmail" class="form-label text-light small font-weight-bold">Adresse Email</label>
                    <input type="email" class="form-control form-control-custom" id="inputEmail" placeholder="nom@exemple.com" aria-label="Adresse Email" autocomplete="email" required>
                </div>
                <div class="mb-4 text-start">
                    <label for="inputPassword" class="form-label text-light small font-weight-bold">Mot de passe</label>
                    <input type="password" class="form-control form-control-custom" id="inputPassword" placeholder="•••••••• (min 6 caractères)" aria-label="Mot de passe" autocomplete="new-password" required>
                </div>
                <button type="submit" id="btnSubmit" class="btn btn-login btn-primary w-100 text-light uppercase">S'inscrire</button>
            </form>
        </section>
    </div>
</main>

<footer class="text-center mt-4">
    <p class="text-secondary small"> Powered by Souflydev </p>
</footer>

<!-- Bootstrap Bundle JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>

<script>
    const form = document.getElementById("registerForm");
    const msgContainer = document.getElementById("message-container");
    const btnSubmit = document.getElementById("btnSubmit");

    form.addEventListener("submit", async (e) => {
        e.preventDefault();
        msgContainer.innerHTML = "";
        
        const pseudo = document.getElementById("inputPseudo").value.trim();
        const email = document.getElementById("inputEmail").value.trim();
        const password = document.getElementById("inputPassword").value;

        if (password.length < 6) {
            msgContainer.innerHTML = `<div class="alert alert-danger py-2 px-3 small mb-0" role="alert"><i class="bi bi-exclamation-triangle-fill me-1"></i> Le mot de passe doit faire au moins 6 caractères.</div>`;
            return;
        }

        btnSubmit.disabled = true;
        btnSubmit.innerHTML = `<span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></span>Inscription...`;

        try {
            const response = await fetch("/register", {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({ pseudo, email, password })
            });

            const data = await response.json();

            if (response.ok && data.success) {
                msgContainer.innerHTML = `<div class="alert alert-success py-2 px-3 small mb-0" role="alert"><i class="bi bi-check-circle-fill me-1"></i> ${data.message}</div>`;
                setTimeout(() => {
                    window.location.href = data.redirect || "/login";
                }, 1500);
            } else {
                msgContainer.innerHTML = `<div class="alert alert-danger py-2 px-3 small mb-0" role="alert"><i class="bi bi-exclamation-circle-fill me-1"></i> ${data.message || "Une erreur est survenue."}</div>`;
                btnSubmit.disabled = false;
                btnSubmit.innerHTML = "S'inscrire";
            }
        } catch (error) {
            console.error("Erreur:", error);
            msgContainer.innerHTML = `<div class="alert alert-danger py-2 px-3 small mb-0" role="alert"><i class="bi bi-x-circle-fill me-1"></i> Impossible de contacter le serveur.</div>`;
            btnSubmit.disabled = false;
            btnSubmit.innerHTML = "S'inscrire";
        }
    });
</script>
</body>
</html>